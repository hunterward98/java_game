package io.github.inherit_this.world;

/**
 * Enum representing different tile material types.
 * Used to determine when tiles should blend (only different types blend).
 */
public enum TileType {
    GRASS,
    STONE,
    SAND,
    PATH,
    WATER
}
